#include "../neuralNetLib/neuralNetwork.h"
#include <thread>
#include <vector>
int main(int argc, char **argv){
    std::vector<float> output;
    std::vector<std::vector<float>> input;
    std::vector<std::vector<float>> weightz;
    int inputNumz = 0;

    neuralNetwork test;
}